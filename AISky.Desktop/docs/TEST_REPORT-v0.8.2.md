# AISky Desktop v0.8.2 测试报告

测试日期：2026-08-05

## 当前结论

首次启动封面、响应式布局、进度事件与 Energy 下载路径已经通过开发构建、真实服务和最终解压包验证，可以发布。

## 界面

- 全新隔离数据目录启动后立即显示 AISky 品牌封面、模型选择、可选密码和初始化进度区。
- 1100×780 窗口显示品牌与初始化左右分栏。
- 850×700 窗口自动隐藏大幅品牌区，改为带 AISky 小标识的单栏布局。
- 两种尺寸下按钮、输入框与进度区均未重叠，低高度窗口可以纵向滚动。
- 初始进度显示 `0 / 120`；工作进程会报告探测、完整下载、当前文件和文件完成事件。

## Energy 真实服务

- 使用访问码 `1234`：成功找到 `20260804_1930` 起报，下载 1 个真实 NetCDF，失败 0。
- 密码留空：成功找到同一起报并识别已有文件，失败 0。
- 验证范围限制为 `+3h` 单文件，避免测试期间重复下载约 10 GB 的完整 120 文件序列。
- Energy 公开直连返回真实 HDF5/NetCDF；未发布文件返回 404，不再进入密码错误分支。

## 自动测试

- Python 语法检查通过。
- 快速数据管线测试通过：15 个进度事件、8 个图层、下载与索引成功。
- 完整数据管线测试通过：同步、缓存清理、损坏 NetCDF、缺失数据、错误密码、网络重试和数据库恢复均通过。
- Release x64 构建通过：0 个警告，0 个错误。

## 发布包

- 文件：`AISky-Desktop-win-x64.zip`
- 大小：174,434,442 字节
- SHA-256：`086bd045d60126e14fa8dd83c819a6bcfab01cab06c2f51c59ed2d204e335b1d`
- 产品版本：`0.8.2+d9c240448b4552abb0e49e1fa2505707cee8828d`
- ZIP 内未发现 NetCDF、预报索引、设置或下载缓存。
- 内置 Python、NumPy 与 netCDF4 导入检查通过。
- 最终 ZIP 从全新目录解压并使用空数据目录启动成功，首先显示新的 AISky 初始化封面。
