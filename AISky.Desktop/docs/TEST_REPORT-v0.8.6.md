# AISky 0.8.6 测试报告

测试日期：2026-08-06

## 构建

- `AISky.Desktop` Release x64：通过，0 个警告，0 个错误
- `AISky.Updater` Release x64：通过，0 个警告，0 个错误
- 主程序文件版本：`0.8.6.0`

## 数据管线

完整测试脚本 `scripts/Test-AISky.ps1` 通过：

- NetCDF 图层读取、索引与下载：通过
- 同步与缓存清理：通过
- 损坏 NetCDF 拒绝入库：通过
- 空数据与缺失数据处理：通过
- 密码错误说明：通过
- 网络失败重试：通过
- 损坏 SQLite 自动恢复：通过

## 桌面体验

- 新数据目录冷启动先显示 AISky 品牌封面：通过
- 无完整 120 时次数据时进入初始化下载界面：通过
- 设置页显示当前数据目录并可选择新目录：通过
- 播放按钮使用内置矢量图形，不依赖系统字体：通过
- 右上角 Energy / SDS 状态以 3.5 秒节奏垂直轮播：通过构建检查
- 更新完成后由独立更新助手重新启动主程序：通过代码路径检查

## 发布产物

- `AISky-Desktop-win-x64.zip`：生成成功
- `AISky-Setup-win-x64.exe`：生成成功
- 主程序与安装器均包含可提取的 32 × 32 AISky 图标
- ZIP 与安装器 SHA-256 校验文件：生成成功
- 本版本按用户要求使用未签名安装器

## 说明

0.8.6 是启动、同步、存储和更新体验的修复版本，不包含台风路径功能。
