# AISky Desktop v0.8.1 测试报告

测试日期：2026-08-05

## 结论

v0.8.1 已通过首次启动、旧缓存隔离、完整序列识别、超宽屏显示、数据管线和发布构建测试，可以发布。

## 首次启动与数据准备

- 全新数据目录：显示模型选择、密码输入和完整预报下载界面，不再直接进入地图。
- 旧版缓存目录：即使残留 AISky-Energy 数据，只要没有新的首次设置完成标记，仍停留在准备界面。
- 完整序列目录：同一模型、同一起报包含 `+3h` 至 `+360h` 共 120 个连续时次并具有完成标记时，正常进入地图。
- 不完整序列：不会保存首次设置完成状态，也不会隐藏准备界面。
- 设置并发：修复主窗口和页面并行初始化时争用 `settings.json.tmp` 的问题；全新目录启动无配置错误。

## 地图与窗口

- JavaScript 语法检查通过。
- 1920×1080 画布下，经纬度视野宽高比为 `120 / 67.5 = 1.7778`，与画布一致。
- 3440×1440 画布下，经纬度视野宽高比为 `160.0556 / 67 = 2.3889`，与画布一致。
- 首次最大化和窗口尺寸变化时会依据 WebView 实际宽高调整可视经纬度范围，未出现地图横向拉伸。

## 构建与数据管线

- Release x64 构建：0 个警告，0 个错误。
- 快速测试：`scripts/Test-AISky.ps1 -Quick` 通过，多次实测约 1.8–2.6 秒。
- 完整数据测试：
  - 数据工作进程与 8 个变量图层冒烟测试通过。
  - 同步、缓存清理与索引更新测试通过。
  - 空索引、损坏 NetCDF、缺失数据、错误密码说明、下载重试和数据库恢复测试通过。

## 发布包

- 文件：`AISky-Desktop-win-x64.zip`
- 大小：174,431,511 字节
- SHA-256：`4c57c59d573712ca7705e65e1b0206a8485dd8cd69e3c613a0fb0c7ec6e84ef8`
- 产品版本：`0.8.1+edbd61d08453476a105d6aee4ac7f76d234105f8`
- 包内未发现 NetCDF、预报索引、设置或下载缓存。
- 解压后的内置 Python、NumPy 与 netCDF4 导入检查通过。
- 解压包使用全新隔离数据目录启动成功，窗口首先显示模型选择、密码输入和“下载完整预报并进入”，没有直接进入旧 AISky-Energy 预报。
