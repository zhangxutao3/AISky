# AISky 0.9.2 测试报告

测试日期：2026-08-06

## 自动测试

- `node --check AISky.Desktop/MapHost/map.js`：通过
- Release x64 增量构建：通过，0 警告、0 错误
- `scripts/Test-AISky.ps1`：通过
  - 数据服务冒烟测试：通过
  - 同步和缓存清理：通过
  - 空索引、损坏 NetCDF、缺失数据、密码错误、网络重试和数据库恢复：通过
  - 台风算法：通过，1 条路径、8 个节点、98% 置信度

## 桌面交互验证

- Energy 为当前模型时，Energy 珊瑚色路径位于最上层，SDS 路径灰化：通过
- 点击 SDS 路径 `+120h` 后，模型、预报选择、地图和时间轴同步切换：通过
- 路径全部三小时时次支持吸附，悬停卡和脉冲预选提示可见：通过
- 点击格点序列曲线后，由 `+3h` 同步跳转至 `+192h`：通过
- 格点曲线悬停显示 `+N 小时`、有效时间和值：通过
- 深色格点抽屉、读数卡、曲线、网格和当前时刻标记：通过
- 风粒子弱风长尾、强风短尾及台风核心减密：通过

## 发布验证

- Release x64 构建：通过
- 完整便携包与每用户安装器生成：通过
- `AISky-Desktop-win-x64.zip` SHA-256：
  `1d976900b9376e33c56dd2fcd4d07741c41e224527b9cc7af404c7d485567bd5`
- `AISky-Setup-win-x64.exe` SHA-256：
  `79e5b17170cf14a37a008057bbcfe7f70db0d87a43907198ec5466fa8d101cf8`
- 打包目录中的应用启动、主窗口响应与地图加载：通过
